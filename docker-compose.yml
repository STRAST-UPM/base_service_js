services:
  web:
    image: ghcr.io/strast-upm/base_service_js:latest
    ports:
      - "30080:8080"
    env_file:
      - .env
    environment:
      - PORT=${PORT:-8080}
      - REGION=${REGION:-unknown}

networks:
  default:
    driver: bridge